import React from 'react'
import navData from'../utils/data';
import navStyles from '../component/Navbar.module.css'

const Navbar = () => {
  return (
    <div>
     {/* <section className={navStyles.parent}>
        <ul>
        {navData.map((ele)=>
        {
        return(<li>{ele.navText}</li>)
        }
        )}
        </ul>
        
     </section> */}
    </div>
  )
}

export default Navbar
